#political_decisions = {
    #commission_tank = {
        picture = ironclad_commissions
        potential = {
            NOT = { iron_steamers = 1 }
            NOT = { has_country_flag = lacking_writing_system }
            NOT = { has_country_flag = tank_commissioned }
        }

        allow = {
            money = 51000
            NOT = { is_disarmed = yes }
            NOT = { has_country_modifier = warship_commissioned }
            any_owned_province = { is_core = THIS }
            war = no
            flintlock_rifles = 1
        }

        effect = {
            random_owned = { limit = { owner = { NOT = { has_global_flag = tank_commissioned } } } set_global_flag = tank_commissioned }
            set_country_flag = tank_commissioned
			random_owned = { limit = { is_core = THIS } sub_unit = { type = tank value = current } }
            treasury = -50000
        }

        ai_will_do = {
            factor = 1

            #modifier = { factor = 0 NOT = { number_of_states = 6 } colonial_nation = no }
        }
    #}
#}